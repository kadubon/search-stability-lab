# Task 004: Checkpoint Resume Reset

This bundled micro-task models a stale checkpoint lineage where a clean resume branch should be recovered.

Target files:

- `state/checkpoint.py`
- `tests/test_checkpoint.py`

Resolution criterion:

- `pytest -q tests/test_checkpoint.py`
