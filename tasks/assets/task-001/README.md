# Task 001: Parser Fallback

This bundled micro-task models a parser fallback bug where a default parser branch is easy to retire too early.

Target files:

- `src/parser.py`
- `tests/test_parser.py`

Resolution criterion:

- `pytest -q tests/test_parser.py`
