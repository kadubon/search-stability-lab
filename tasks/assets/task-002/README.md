# Task 002: Auth Refresh Path

This bundled micro-task models stale legacy continuation where a refresh branch should survive long enough to recover the session.

Target files:

- `auth/session.py`
- `tests/test_session.py`

Resolution criterion:

- `pytest -q tests/test_session.py`
