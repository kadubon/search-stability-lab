# Task 005: Serializer Defaults

This bundled micro-task models a serializer fallback bug where a cheaper same-family default branch should be preserved.

Target files:

- `serialize/config.py`
- `tests/test_serialize.py`

Resolution criterion:

- `pytest -q tests/test_serialize.py`
