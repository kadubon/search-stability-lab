# Task 006: Planner Reset

This bundled micro-task models stale planner state where a reset should surface the adequate replan branch.

Target files:

- `planner/state.py`
- `tests/test_planner_state.py`

Resolution criterion:

- `pytest -q tests/test_planner_state.py`
